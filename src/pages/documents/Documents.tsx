import {FC} from 'react';
import Layout from "../../components/layout";

const Documents: FC = () => {
    return (
        <Layout>
             Документы
        </Layout>
    );
};

export default Documents;